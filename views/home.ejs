<% include partials//header.ejs %>
    <h1>Home</h1>
    <p>
        <%=  parragraph  %>
    </p>
    <% postArray.forEach(function(post){ %>
        <h1>
            <%= post.title %>
        </h1>
        <p>
            <%=  post.body.substring(0, 100)+" ..."  %> <a href="/posts/<%=post._id%>">Read more.</a>
        </p>
        <%})%>
            <% include partials//footer.ejs %>